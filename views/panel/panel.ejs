<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://bootswatch.com/5/vapor/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.8.1/font/bootstrap-icons.css">
    <title>RasiaPVP - Panel</title>
</head>
<body style="height: 100vh;">
    <nav class="navbar navbar-expand-lg navbar-dark bg-primary">
        <div class="container">
          <a class="navbar-brand">RasiaPVP Dashboard</a>
          <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarColor01" aria-controls="navbarColor01" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
          </button>
      
          <div class="collapse navbar-collapse" id="navbarColor01">
            <ul class="navbar-nav ms-auto">
                <li class="nav-item">
                    <span class="nav-link">Logged in as <%- locals.name %> <img src="https://minecraft-api.com/api/skins/<%= locals.name%>/head"></span>
                </li>
                <li class="nav-item"><a href="../../" class="nav-link"><i class="bi bi-box-arrow-right"></i></a></li>
            </ul>
            </div>
          </div>
        </div>
      </nav>

      <div class="container" style="padding-top: 50px;">
          <div class="row row-cols-3 row-cols-sm-1">
                <% locals.rewards.forEach(el => { %>
                    <div class="p-3 col-12 col-md-6 col-lg-4">
                        <div class="card h-100 border-<%= el.color || 'primary' %>">
                          <div class="card-header"><%= el.name %></div>
                            <div class="card-body">
                              <p class="card-text">
                                Reward: <%= el.reward %><br>Time: <%= `${Math.floor(el.time / 60)} minutes` %>
                              </p>
                              <form action="/panel/farm" method="post">
                                    <input type="text" name="task" value="<%= el.index %>" style="visibility: hidden; width: 0px; border: 0px; padding: 0px;">
                                    <input type="text" name="name" value="<%= locals.name %>" style="visibility: hidden; width: 0px; border: 0px; padding: 0px;">
                                    <button type="submit" class="btn btn-<%= el.color || 'primary' %>">Start</button>
                                </form>
                            </div>
                          </div>
                    </div>
                <% }) %>
          </div>
      </div>

      <footer class="footer mt-auto py-3 bg-dark fixed-bottom">
        <div class="container">
          <span class="text-muted">made by <a href="https://github.com/ploxxxy" target="_blank" title="Discord: ploxxy#6090">ploxxy</a></span>
        </div>
      </footer>
      
</body>
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-ka7Sk0Gln4gmtz2MlQnikT1wXgYsOg+OMhuP+IlRH9sENBO0LRn5q+8nbTov4+1p" crossorigin="anonymous"></script>
</html>